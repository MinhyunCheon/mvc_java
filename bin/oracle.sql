CREATE TABLE USER_TBL(
	ID		VARCHAR2(50) PRIMARY KEY
	, PWD	VARCHAR2(50) NOT NULL
	, NAME	VARCHAR2(50) NOT NULL
	, POINT	NUMBER DEFAULT 1000
);

SELECT * FROM USER_TBL;

INSERT INTO USER_TBL VALUES (?, ?, ?, ?);

UPDATE USER_TBL SET POINT = ? WHERE ID = ?;

DELETE FROM USER_TBL WHERE ID = ?;

CREATE TABLE BBS_TBL(
	SEQ			NUMBER	PRIMARY KEY
	, SUBJECT	VARCHAR(50)
	, CONTENT	VARCHAR(1000)
	, WRITER	VARCHAR(50)
	, REGDATE	DATE
	, VIEWCNT	NUMBER
);

CREATE SEQUENCE BBS_SEQ;

INSERT INTO BBS_TBL
VALUES (
	BBS_SEQ.NEXTVAL
	, '공지'
	, '내용 없음'
	, 'Cheon'
	, SYSDATE
	, 0
);

SELECT * FROM BBS_TBL;
SELECT SEQ, SUBJECT, CONTENT, WRITER, TO_CHAR(REGDATE, 'YYYY-MM-DD'), VIEWCNT FROM BBS_TBL ORDER BY SEQ DESC;